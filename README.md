# install dependencies
npm i

# start devtools
npm run devtools

# start development 
npm run start

# build electron application for production
npm run make


# lint all TS/Vue component files in `src/`
npm run lint
